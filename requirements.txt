# Even better: Use newer stable version
cat > requirements.txt << 'EOF'
# RFL-HRV1.0 - Modern stable versions
qiskit==1.0.2
qiskit-aer==0.13.3  # Newer but still compatible with qiskit 1.0.2
numpy==1.24.3
scipy==1.10.1
matplotlib==3.7.1
EOF

pip install -r requirements.txt
python validation_demo.py
